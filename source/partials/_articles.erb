<section class="article list">
  <ul>
    <% articles.each do |article| %>
      <li><%= link_to article.title, article.url %> - <%= article.date.strftime "%B %d, %Y" %></li>
    <% end %>
  </ul>
</section>
