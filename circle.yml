machine:
  ruby:
    version: 2.3.3

dependencies:
  cache_directories:
    - "vendor/bundle"

test:
  override:
    - JEKYLL_ENV=production bundle exec jekyll build

deployment:
  github:
    branch: feature/jekyll
    commands:
      - bash circle_deploy.sh
