webpackJsonp([0x9ecfbe6d189],{462:function(e,l){e.exports={data:{site:{siteMetadata:{title:"Life is a YOLO game",author:"Henry Wu"}},markdownRemark:{id:"/home/circleci/project/src/pages/twil-3.md absPath of file >>> MarkdownRemark",html:'<h2>瑣事</h2>\n<h3><a href="https://lebab.io/">Lebab</a></h3>\n<p>應該不太有人會有這個需求，要把 ECMAScript 5 轉成 Babel。我的情境是 Middleman 會很雞婆地在部署前試著處理 JavaScript，卻無法處理 ECMAScript 6 的語法。然後個人感覺 Middleman 社群對於支援 ECMAScript 6 這件事也是愛做不做的，所以我只好先用 <a href="https://babeljs.io/repl/">Babel REPL</a> 把 Babel 轉成 JavaScript 讓 Middleman 可以成功部署。</p>\n<p>後來想想還是把 Daily UI 的成果放在 <a href="https://codepen.io/">Codepen</a> 好了，畢竟人家都幫你準備好 SCSS + Babel 的環境，不用自己架還不用那真的是太對不起自己了。所以呢，我決定把原來 ECMAScript 5 的程式碼轉換回 Babel。以免訪客以為我只會寫 ECMAScript 5 卻不會 ECMAScript 6。這件事很重要啊！如果訪客是你下一份工作的面試官呢？</p>\n<p>有發現 Lebab 是 Babel 倒過來嗎？</p>\n<h3><a href="http://stackoverflow.com/a/16892086">Change 2-space indent to 4-space in vim</a></h3>\n<p>vim 快速將四格空白替換成兩格空白。其實原理就是切換 vim 的參數，配合 <code class="language-text">retab</code> 指令強制縮排。看起來多此一舉，但好處是我們不需要另外安裝 plugin，因為偶爾才會遇到類似的情形。</p>\n<h2>Daily UI</h2>\n<h3><a href="http://codepen.io/henry40408/full/vXoMxj/">Day 4: Calculator</a></h3>\n<p>計算機，大家已經做到爛掉了的題目。個人決心不要再用傳統的排版樣式，於是在 Dribble 上找到這個把四則運算符號移到數字鍵盤上方的版本 <a href="https://dribbble.com/shots/2296696-Daily-UI-Day-4-Calculator">Daily UI - Day 4: Calculator</a>。</p>\n<h2><a href="https://github.com/henry40408/leetcode">LeetCode 大冒險</a></h2>\n<p>先解 alogrithm 的部分，由於我 C/C++ 都忘得差不多了，主要使用 Python 解題。</p>\n<h3><a href="https://leetcode.com/problems/two-sum">Two Sum</a></h3>\n<ul>\n<li>利用 <code class="language-text">dict</code> lookup 嚇死人地快<sup id="fnref-1"><a href="#fn-1" class="footnote-ref">1</a></sup>的特性，以 <code class="language-text">dict</code> 解之。</li>\n<li><code class="language-text">matrix</code> 是一個 <code class="language-text">dict</code>，以數字為 key，該數字在 list 中的 index 為 value。</li>\n<li>\n<p>iterate 整個 list</p>\n<ul>\n<li>假設目前的數字是 7，目標是 9，已知是兩個數字相加，以 <code class="language-text">in</code> operator 找找看 <code class="language-text">dict</code> 裡有沒有 2。</li>\n<li>有，直接 <code class="language-text">dict</code> lookup，回傳對應的數字 (2) 的位置與目前數字 (7) 的位置。</li>\n<li>沒有，把目前的數字為 key，位置為 value 放入 <code class="language-text">dict</code>，等待相對應的數字出現。</li>\n</ul>\n</li>\n</ul>\n<h3><a href="https://leetcode.com/problems/add-two-numbers">Add Two Numbers</a></h3>\n<ul>\n<li>\n<p>第一次真的看不懂，為什麼 <code class="language-text">4 + 6</code> 會等於 <code class="language-text">0</code>？然後 <code class="language-text">4 + 3</code> 會等於 <code class="language-text">8</code>？</p>\n<ul>\n<li>看了 Input 與 Output 快第十次，才想到要回去看題目。</li>\n<li>注意這句話 The digits are stored in reverse order and each of their nodes contain a <strong>single</strong> digit.</li>\n<li>也就是說一個 element 只會放一位數，超過一位數就要讓下一位進位。</li>\n<li>考慮一下 edge case，一位數的最大值是 <code class="language-text">9</code>，<code class="language-text">9 + 9 = 18</code>，不會有要讓下一位 <code class="language-text">+2</code> 的情況發生。</li>\n</ul>\n</li>\n<li>直接用 recursion 打死，code 既漂亮又好理解。</li>\n<li>麻煩的地方在測試，linked list 在 Python 中反而不好處理。</li>\n</ul>\n<h3><a href="https://leetcode.com/problems/longest-substring-without-repeating-characters">Longest Substring Without Repeating Characters</a></h3>\n<blockquote>\n<p>Recursion 不是卍解</p>\n</blockquote>\n<ul>\n<li>\n<p>遞迴好用，但在這題踢到鐵板。</p>\n<ul>\n<li>CPython 的遞迴層數是有限制的，而且要修改也不好改。</li>\n<li>個人估計 LeetCode 也不會允許你去動那個限制。</li>\n<li>最後的解法是回去用迴圈。</li>\n</ul>\n</li>\n<li>設定一個暫存用的字串 <code class="language-text">substring = &quot;&quot;</code>，暫存目前最大長度 <code class="language-text">longest = 0</code>。</li>\n<li>\n<p>走訪字串的每個字元，設為 <code class="language-text">c</code>。</p>\n<ul>\n<li>如果 <code class="language-text">substring</code> 包含 <code class="language-text">c</code>，把 <code class="language-text">substring</code> 從 <code class="language-text">c</code> 斷開，然後把 <code class="language-text">c</code> 接到 <code class="language-text">substring</code> 後面。</li>\n<li>考慮以下幾個情形：</li>\n<li><code class="language-text">c = d, substring = dabc</code> (<code class="language-text">c</code> 在最前面) 則 <code class="language-text">substring = abcd</code></li>\n<li><code class="language-text">c = d, substring = adbc</code> (<code class="language-text">c</code> 在 <code class="language-text">substring</code> 中間) 則 <code class="language-text">substring = bcd</code></li>\n<li><code class="language-text">c = d, substring = abcd</code> (<code class="language-text">c</code> 在 <code class="language-text">substring</code> 最後) 則 <code class="language-text">substring = d</code></li>\n<li>如果 <code class="language-text">substring</code> 完成一回合之後的長度 <strong>大於</strong> <code class="language-text">longest</code>，更新 <code class="language-text">longest</code>。</li>\n</ul>\n</li>\n<li>最後的 <code class="language-text">longest</code> 就是答案。</li>\n</ul>\n<h3><a href="https://leetcode.com/problems/median-of-two-sorted-arrays">Median of Two Sorted Arrays</a></h3>\n<ul>\n<li>這題算我作弊，但嚴格來說也不能算作弊。</li>\n<li>我直接用 Python 內建的 <code class="language-text">sorted</code> 把 list 們合併 (<code class="language-text">+</code>) 之後直接取中位數。</li>\n<li>對 list 下 <code class="language-text">sorted</code> 的時間複雜度平均 <code class="language-text">O(n log n)</code> 最差 <code class="language-text">O(n log n)</code>。</li>\n<li>我有想過需不需要改用 generator 避免測資太大筆導致記憶體爆掉，事後想一想如果題目有一筆超級巨大的就會改用 generator 而不是 list 傳入了，否則在程式一開始執行的時候就會出問題。</li>\n</ul>\n<h3><a href="https://leetcode.com/problems/longest-palindromic-substring">Longest Palindromic Substring</a></h3>\n<ul>\n<li>\n<p>這題是大魔王，讓我想了自己的解法想了足足三天。</p>\n<ul>\n<li>卡住的點在於當字串的長度是偶數時，必須把中心點放在字元之間，等於程式必須考慮字元 (<code class="language-text">n</code>) + 字元間距 (<code class="language-text">n-1</code>) 兩種情形 (<code class="language-text">2n-1</code>)。</li>\n<li>然後直觀解法一個字元一個字元比對，一定會超時。</li>\n<li>最後實在想不出來，只好問 Google 大神了。</li>\n</ul>\n</li>\n<li>最後採用 Manacher’s Algorithm。</li>\n</ul>\n<h2>MacBook Air 送修前的轉移作業</h2>\n<p>由於 MacBook Air 的電池看起來快掛了，在送修之前我會把主機內的資料清空，因此勢必要另外開闢一個開發環境來繼續開發尚未完成的專案與部落格的更新。目前選擇的是 Ubuntu，以下是我考慮的幾個重點：</p>\n<ul>\n<li><strong>dotfile 的同步</strong> 我現在都用 vim 進行日常開發，shell 則是 zsh，dotfile 的同步是一定要的。所謂的 dotfile 指的是前面帶有 <code class="language-text">.</code> 的設定檔，一般都放在 home directory 下，例如 <code class="language-text">.zshrc</code>、<code class="language-text">.vimrc</code>、<code class="language-text">.tmux.conf</code> 都是 dotfile。雖然因為不爽 Sublime Text 一直跳購買通知出來所以買了 license，但現在幾乎沒有再去開過 Sublime 了，次數少到就算現在移除了 Sublime 也不會對我的日常開發造成干擾。</li>\n<li><strong>SSH key 的同步</strong> 這件事情我原本還想說用 USB 傳一傳就搞定了，卻在逛 <a href="https://github.com/unixorn/awesome-zsh-plugins">awesome-zsh-plugins</a> 時看到 <a href="https://github.com/StackExchange/blackbox">BlackBox</a> 這個 Plugin，可對 Git repository 加密，讓 SSH key 可以安全地透過 GitHub 同步。</li>\n<li><strong>GitHub Pages + Travis 佈署部落格</strong> 這個算是 bonus，因為我大可以在 Ubuntu 上直接更新部落格之後直接更新。但我想要潮一點，透過 iPad 更新部落格，雖然不一定之後會這麼做就是了，才把腦筋動到 CI 身上。</li>\n</ul>\n<h3>跨裝置同步 dotfiles</h3>\n<p><a href="https://developer.atlassian.com/blog/2016/02/best-way-to-store-dotfiles-git-bare-repo/">The best way to store your dotfiles: A bare Git repository</a></p>\n<p>我目前手上有三支 dotfile 需要同步 <code class="language-text">.zshrc</code>、<code class="language-text">.vimrc</code> 與 <code class="language-text">.tmux.conf</code>。以前的作法是使用 <code class="language-text">gist</code> 這個 gem 加上自己寫的 alias command 去完成。假設今天我要備份 <code class="language-text">.zshrc</code>，我就先 alias 一個 command <code class="language-text">alias sync-zshrc=&#39;gist -u xoxoxoxo $HOME/.zshrc&#39;</code>，<code class="language-text">xoxoxoxo</code> 是 <code class="language-text">.zshrc</code> 的 gist ID，要備份的時候我只要在 shell 下 <code class="language-text">sync-zshrc</code> 就備份完成了。</p>\n<p>問題是，<strong>這樣的流程在備份上很簡單，但還原時就很麻煩了</strong>。</p>\n<p>因為我必須有幾個 dotfile 就要記幾個 gist ID，而且擔心 gist ID 外洩我還另外把 alias command 獨立成一個檔案，也就是備份的命令沒有備份到，假如有一天我要還原這些 dotfile，就要一個一個去 gist 上把它們的 gist ID 找回來，重新定義一次 alias command。</p>\n<p>想到就很煩。</p>\n<p>透過 Git bare repository 備份 dotfile 有幾個好處：</p>\n<ul>\n<li><strong>一個 Git repository 打死所有 dotfile</strong> 不管日後新增或刪除 dotfile 都不會影響備份或還原的流程。</li>\n<li><strong>備份、還原用的 alias command 可以直接寫死在 shell 的 dotfile 裡</strong> 因為不會帶任何敏感參數。</li>\n<li><strong>備份就是 commit + push，還原則是 pull + checkout</strong> 都是 Git 的流程與指令，不需要學新東西。</li>\n</ul>\n<h3>BlackBox 安全共享敏感檔案</h3>\n<p><a href="https://github.com/StackExchange/blackbox">StackExchange/blackbox</a></p>\n<p>BlackBox 的原理很簡單，即使用 GPG 加密來保護敏感檔案。相較於一般的加密方法，GPG 加密反其道而行， <strong>以公鑰加密、以私鑰解密</strong>。由於 repository 的 README 寫的非常簡略，在此重新整理一次如何使用這個工具。</p>\n<p>在開始之前，有幾件事情要特別提醒：</p>\n<ul>\n<li>GPG key-pair 應該以機器 (host) 為單位，key-pair 不應該以任何形式同步到兩台機器上</li>\n<li>要分享敏感資料給新機器或新使用者，<strong>必須仰賴任何一台已經可以解密敏感檔案的機器重新加密檔案</strong></li>\n<li>換句話說，<strong>如果有辦法解密檔案的機器都無法使用，那麼這些敏感檔案就無法被破解了</strong>，這算是在方便性與安全性上取得妥協</li>\n</ul>\n<p>假設現在有兩個使用者：Alice 與 Bob。</p>\n<h4>Alice 備份自己的敏感檔案</h4>\n<ul>\n<li>Alice 在自己的電腦上產生一對 GPG key-pair，分別是私鑰 <em>SK_alice</em> 公鑰 <em>PK_alice</em> <code class="language-text">gpg --gen-key</code></li>\n<li>Alice 將自己加入受信任的資料庫中 <code class="language-text">blackbox_addadmin</code></li>\n<li>Alice 使用 <em>PK_alice</em> 將敏感檔案加密 <code class="language-text">blackbox_register_new_file</code></li>\n<li>Alice 將加密後的敏感檔案上傳到 GitHub <code class="language-text">git push origin master</code></li>\n</ul>\n<h4>Alice 還原自己的敏感檔案</h4>\n<ul>\n<li>\n<p>新電腦</p>\n<ul>\n<li>Alice 在新電腦上產生一對 GPG key-pair，分別是私鑰 <em>SK<em>new</em>alice</em> 與公鑰 <em>PK<em>new</em>alice</em> <code class="language-text">gpg --gen-key</code></li>\n<li>Alice 將存放敏感檔案的 repository clone 下來，加入 <em>PK<em>new</em>alice</em> <code class="language-text">blackbox_addadmin KEYNAME</code></li>\n<li>Alice 將 repository 同步回 GitHub <code class="language-text">git commit</code> <code class="language-text">git push origin master</code></li>\n</ul>\n</li>\n<li>\n<p>舊電腦</p>\n<ul>\n<li>Alice 同步 repository <code class="language-text">git pull</code></li>\n<li>Alice 確定新電腦的公鑰有進入 repository <code class="language-text">gpg --homedir=keyrings/live --list-keys</code></li>\n<li>Alice 匯入 repository 中兩支公鑰 <em>PK_alice</em> 與 <em>PK<em>new</em>alice</em> <code class="language-text">gpg --import keyrings/live/pubring.gpg</code></li>\n<li>Alice 接下來可以使用這兩隻公鑰重新加密敏感檔案，讓新電腦可以使用 <em>SK<em>new</em>alice</em> 解密 <code class="language-text">blackbox_update_all_files</code></li>\n<li>Alice 將 repository 同步回 GitHub <code class="language-text">git commit</code> <code class="language-text">git push origin master</code></li>\n</ul>\n</li>\n<li>\n<p>新電腦</p>\n<ul>\n<li>Alice 同步 repository <code class="language-text">git pull</code></li>\n<li>Alice 解密敏感檔案 <code class="language-text">blackbox_decrypt_all_files</code></li>\n</ul>\n</li>\n</ul>\n<h4>Alice 與 Bob 共享敏感檔案</h4>\n<ul>\n<li>\n<p>Bob</p>\n<ul>\n<li>Bob 在新電腦上產生一對 GPG key-pair，分別是私鑰 <em>SK_bob</em> 與公鑰 <em>PK_bob</em> <code class="language-text">gpg --gen-key</code></li>\n<li>Bob 將存放敏感檔案的 repository clone 下來，加入 <em>PK_bob</em> <code class="language-text">blackbox_addadmin KEYNAME</code></li>\n<li>Bob 將 repository 同步回 GitHub <code class="language-text">git commit</code> <code class="language-text">git push origin master</code></li>\n</ul>\n</li>\n<li>\n<p>Alice</p>\n<ul>\n<li>Alice 同步 repository <code class="language-text">git pull</code></li>\n<li>Alice 確定新電腦的公鑰有進入 repository <code class="language-text">gpg --homedir=keyrings/live --list-keys</code></li>\n<li>Alice 匯入 repository 中兩支公鑰 <em>PK_alice</em> 與 <em>PK_bob</em> <code class="language-text">gpg --import keyrings/live/pubring.gpg</code></li>\n<li>Alice 接下來可以使用這兩隻公鑰重新加密敏感檔案，讓 Bob 可以使用 <em>SK_bob</em> 解密 <code class="language-text">blackbox_update_all_files</code></li>\n<li>Alice 將 repository 同步回 GitHub <code class="language-text">git commit</code> <code class="language-text">git push origin master</code></li>\n</ul>\n</li>\n<li>\n<p>Bob</p>\n<ul>\n<li>Bob 同步 repository <code class="language-text">git pull</code></li>\n<li>Bob 解密敏感檔案 <code class="language-text">blackbox_decrypt_all_files</code></li>\n</ul>\n</li>\n</ul>\n<p>由以上可以看到，<strong>Alice 自己在兩台電腦或是跟 Bob 共享敏感檔案的流程基本上是一模一樣的</strong>。</p>\n<h3>GitHub Pages + Travis 打造全自動的部落格佈署流程</h3>\n<p><a href="https://jahed.io/blog/2015/05/31/automating-deployments-to-github-pages-with-middleman-and-travis-ci/">Automating Deployments to GitHub Pages with Middleman and Travis CI</a></p>\n<p>如果要在 iPad 上更新部落格，我現在假設的情境是：</p>\n<ul>\n<li>無法使用 command line</li>\n<li>也沒有機器可以讓我直接在 iPad 上 SSH 進去輸入 middleman 的指令</li>\n</ul>\n<p>所以我只能在 iPad 上使用編輯器編輯 Markdown 檔案，編輯完之後使用 Git client 同步回 GitHub。至於這中間編譯成靜態網站的流程有很多方式可以達成，例如使用惡名昭彰的 AWS Lambda、或是另外跑一個 Heroku app 去監視 GitHub 都是一種，但都太過繁瑣。</p>\n<p>於是我把腦筋動到 CI 身上。佈署的具體流程是這樣的：</p>\n<ol>\n<li>Travis 同步 repository</li>\n<li>Travis 取得有能力存取 GitHub 的 SSH key</li>\n<li>執行 Middleman 相關指令將靜態網站編譯出來</li>\n<li>Travis 使用 SSH key 將編譯出來的靜態網站同步回 GitHub</li>\n</ol>\n<p>這個流程中最麻煩的是如何與 Travis 安全地交換 SSH key。這個流程需要配合 Travis 的 client 去達成，但只要執行一次就可以了。</p>\n<ol>\n<li>安裝 Travis client</li>\n<li>產生 SSH key</li>\n<li>將 SSH key 加入 GitHub</li>\n<li>使用 <code class="language-text">travis encrypt</code> 加密 SSH key</li>\n<li>此時會在 Travis 那邊加入兩個環境變數，用來在 Travis 端解密 SSH key</li>\n<li>在 repository 會留下被加密過的 SSH key <code class="language-text">deploy_key.enc</code></li>\n<li>過程非常安全，基本上連下指令的人都不會知道那兩個環境變數具體的值是什麼</li>\n<li>刪除 SSH key</li>\n<li>把加密過的 SSH key 上傳到 GitHub</li>\n<li>Travis 會自動開始建制作業</li>\n</ol>\n<p>除了文章中提到的步驟與設定，我還有在 Travis 上多做一些設定：</p>\n<ul>\n<li><strong>[on] Build only if .travis.yml is present</strong> 否則 Travis 會試圖建制 <code class="language-text">master</code> branch，由於 <code class="language-text">master</code> branch 上是靜態網站的結果，因此一定會建制失敗</li>\n<li><strong>[on] Build pushes</strong> 不開 Travis 就不會動</li>\n<li><strong>[on] Limit concurrent jobs? [1]</strong> 一般如果跑測試，提高這個數值有助於加快測試流程，因為每次的測試彼此之間是獨立的。但由於現在建制會影響到 GitHub 上的內容，我把它設定成 1 避免重複建制</li>\n<li><strong>[off] Build pull requests</strong> 不建制 Pull Requests，一定要更新到 <code class="language-text">development</code> branch 上才會啟動建制，理由同 <code class="language-text">Limit concurrent jobs?</code></li>\n</ul>\n<p>目前我已經將 Travis 的佈署流程應用到部落格上了 <a href="https://github.com/henry40408/henry40408.github.io">henry40408/henry40408.github.io</a>。</p>\n<div class="footnotes">\n<hr>\n<ol>\n<li id="fn-1">\n<p><a href="https://wiki.python.org/moin/TimeComplexity%EF%BC%8Cdict">https://wiki.python.org/moin/TimeComplexity，dict</a> 的 Get Item 平均 <code class="language-text">O(1)</code>，最差 <code class="language-text">O(n)</code>。</p>\n<a href="#fnref-1" class="footnote-backref">↩</a>\n</li>\n<li id="fn-2">\n<p>例如 henry40408.com 這樣的 domain name</p>\n<a href="#fnref-2" class="footnote-backref">↩</a>\n</li>\n</ol>\n</div>',frontmatter:{title:"TWIL #3",date:"November 15, 2016"}}},pathContext:{slug:"/twil-3/",previous:{fields:{slug:"/twil-2/"},frontmatter:{title:"TWIL #2"}},next:{fields:{slug:"/twil-4/"},frontmatter:{title:"TWIL #4"}}}}}});
//# sourceMappingURL=path---twil-3-83d3a9cd9d5f1260603f.js.map